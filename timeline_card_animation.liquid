Preview:https://prnt.sc/94G3gxtr-QDO

<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <link href="https://fonts.cdnfonts.com/css/outfit" rel="stylesheet">
                
    <style>
       

        /* Spacing sections for scroll demo */
        .spacer {
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            color: #666;
        }

        .timeline-section {
            background: #F9F9F9;
            padding: 90px 0px;
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }

       .timeline-section .container {
    max-width: 1440px;
    width: 100%;
    margin: 0 auto;
    padding: 0 25px;
}
        .section-header {
            text-align: center;
            margin-bottom: 60px;
            opacity: 0;
            transform: translateY(-30px);
            transition: all 0.8s ease;
        }

        .timeline-section.animated .section-header {
            opacity: 1;
            transform: translateY(0);
        }

       .section-header h1 {
    color: #000;
    margin: 0 0 20px;
    font-weight: 700;
    font-family: Outfit;
    font-weight: 600;
    font-style: SemiBold;
    font-size: 45px;
    leading-trim: NONE;
    line-height: 40px;
    letter-spacing: 0px;
    text-align: center;
    vertical-align: middle;
    text-transform: uppercase;
}

       .section-header p {
    font-family: Outfit;
    font-weight: 400;
    font-style: Regular;
    font-size: 18px;
    leading-trim: NONE;
    line-height: 30px;
    letter-spacing: 0%;
    text-align: center;
    text-transform: capitalize;
    color: #000;
    margin: 0;
}
        .timeline {
            position: relative;
            padding: 40px 0;
        }

        /* Vertical line */
       .timeline::before {
    content: '';
    position: absolute;
    left: 50%;
    top: 0;
    bottom: 0;
    width: 4px;
    transform: translateX(-50%);
    background: #9F0000;
    border-radius: 2px;
}

        .timeline-item {
            position: relative;
            margin-bottom: 80px;
            opacity: 0;
            transform: translateY(50px);
            transition: all 0.8s ease;
        }

        .timeline-section.animated .timeline-item {
            opacity: 1;
            {% comment %} transform: translateY(0); {% endcomment %}
        }

        /* Stagger animation delays */
        .timeline-item:nth-child(1) { transition-delay: 0.2s; }
        .timeline-item:nth-child(2) { transition-delay: 0.5s; }
        .timeline-item:nth-child(3) { transition-delay: 0.8s; }
        .timeline-item:nth-child(4) { transition-delay: 1.1s; }

       .timeline-content {
    width: 45%;
    background: white;
    padding: 24px 38px;
    border-radius: 5px;
    position: relative;
    border: 1px solid #CECECE;
}

        /* Alternate sides */
        .timeline-item:nth-child(odd) .timeline-content {
            margin-right: auto;
            margin-left: 3%;
        }

       .timeline-item:nth-child(even) .timeline-content {
    margin-left: auto;
    margin-right: 3%;
}
        /* Timeline dot */
       .timeline-dot {
    position: absolute;
    left: 50%;
    top: 30px;
    width: 20px;
    height: 20px;
    background: #9F0000;
    border: 4px solid #ffffff;
    border-radius: 50%;
    transform: translateX(-50%) scale(0);
    transition: transform 0.5s ease;
    z-index: 10;
    display: block !important;
}
        .timeline-section.animated .timeline-dot {
            transform: translateX(-50%) scale(1);
        }

        .timeline-item:nth-child(1) .timeline-dot { transition-delay: 0.2s; }
        .timeline-item:nth-child(2) .timeline-dot { transition-delay: 0.5s; }
        .timeline-item:nth-child(3) .timeline-dot { transition-delay: 0.8s; }
        .timeline-item:nth-child(4) .timeline-dot { transition-delay: 1.1s; }

        /* Stage label */
        .stage-label {
    display: inline-block;
    padding: 0;
    background: transparent;
    color: white;
    border-radius: 20px;
    font-size: 0.85rem;
    font-weight: 600;
    margin-bottom: 12px;
    font-family: Outfit;
    font-weight: 700;
    font-style: Bold;
    font-size: 18px;
    leading-trim: NONE;
    line-height: 28px;
    letter-spacing: 0px;
    text-align: right;
    color: #9F0000;
       margin: 0;
    display: flex;
    justify-content: end;
}

       .timeline-content h3 {
    color: #2d3748;
    margin-bottom: 12px;
    font-weight: 700;
    font-family: Outfit;
    font-weight: 700;
    font-style: Bold;
    font-size: 24px;
    leading-trim: NONE;
    line-height: 32px;
    letter-spacing: 0px;
    text-align: right;
    margin: 0;
}
.timeline-content p {
    font-size: 0.95rem;
    color: #4a5568;
    line-height: 1.6;
    font-family: Outfit;
    font-weight: 400;
    font-style: Regular;
    font-size: 16px;
    leading-trim: NONE;
    line-height: 26px;
    letter-spacing: 0px;
    text-align: right;
    margin: 0;
}
.timeline-item:nth-child(even) .timeline-content span.stage-label {
    display: flex;
    justify-content: start;
}

.timeline-item:nth-child(even) .timeline-content h3 {
    text-align: start;
  
}

.timeline-item:nth-child(even) .timeline-content p {
    text-align: start;
   
}

        /* Responsive */
        @media (max-width: 768px) {
            .timeline::before {
                left: 30px;
            }

            .timeline-content {
                width: calc(100% - 80px);
                margin-left: 80px !important;
                margin-right: 0 !important;
            }

            .timeline-dot {
                left: 30px;
            }

                .section-header h1 {
        font-size: 32px;
    }
     .stage-label {
        justify-content: start;
    }
.timeline-content h3 {
    text-align: start;

}

.timeline-content p {
    text-align: start;
}
.timeline-item {
    
    margin-bottom: 60px;
 
}
.timeline-section {
  
    padding: 50px 0px;
  
}
        }



        @media(max-width:480px){
        .timeline-content {
 
    padding: 20px 20px;
  

}

.timeline-item {
    margin-bottom: 50px;

}

.timeline-section {
  
    padding: 30px 0px;
  
}

   .timeline-content {
        width: calc(100% - 45px);
        margin-left: 45px !important;
        margin-right: 0 !important;
    }
    .timeline-dot {
        left: 13px;
    }
    .timeline::before {
        left: 13px;
    }

        .section-header h1 {
        font-size: 24px;
        margin: 0 0 5px;
    }

.section-header p {
  
    font-size: 17px;
   
}

.stage-label {
    font-size: 16px;

}
.timeline-content h3 {
    font-size: 20px;
}
.timeline {
    position: relative;
    padding: 40px 0 60px;
}

        }

        /* Animation pulse effect for dots */
        @keyframes pulse {
            0% {
                box-shadow: 0 0 0 0 #9F0000;
            }
            50% {
                box-shadow: 0 0 0 15px rgba(102, 126, 234, 0);
            }
            100% {
                box-shadow: 0 0 0 0 rgba(102, 126, 234, 0);
            }
        }

        .timeline-section.animated .timeline-dot {
            animation: pulse 2s infinite;
        }
    </style>


    <!-- Timeline Section -->
    <section class="timeline-section" id="timelineSection">
        <div class="container">
            <div class="section-header">
                <h1>START WITH THESE</h1>
                <p>From Curiosity To Mastery How Personal Experience Became A Mission To Help Thousands Of Men</p>
            </div>

            <div class="timeline">
                <!-- Timeline Item 1 -->
                <div class="timeline-item">
                    <div class="timeline-dot"></div>
                    <div class="timeline-content">
                        <span class="stage-label">Early Days</span>
                        <h3>From Customer to Advocate</h3>
                        <p>Started as a curious customer exploring male enhancement devices. Early experiences with cheap, ineffective products led to discovering what actually works and why.</p>
                    </div>
                </div>

                <!-- Timeline Item 2 -->
                <div class="timeline-item">
                    <div class="timeline-dot"></div>
                    <div class="timeline-content">
                        <span class="stage-label">Discovery</span>
                        <h3>The Biohacking Journey Begins</h3>
                        <p>Dove deep into understanding the body as a system. Tested protocols, measured outcomes, and refined approaches before biohacking became mainstream.</p>
                    </div>
                </div>

                <!-- Timeline Item 3 -->
                <div class="timeline-item">
                    <div class="timeline-dot"></div>
                    <div class="timeline-content">
                        <span class="stage-label">Connection</span>
                        <h3>Meeting Dr. Joel Kaplan</h3>
                        <p>Found Dr. Joel Kaplan's devices—products that made biological sense. Later learned Joel was family, but credibility came from understanding the science, not the connection.</p>
                    </div>
                </div>

                <!-- Timeline Item 4 -->
                <div class="timeline-item">
                    <div class="timeline-dot"></div>
                    <div class="timeline-content">
                        <span class="stage-label">Today</span>
                        <h3>Building a Movement</h3>
                        <p>Now running multiple brands helping men reclaim control of their health, performance, and confidence through proven systems and quality products.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

  

    <script>
        $(document).ready(function() {
            // Intersection Observer options
            const options = {
                root: null,
                rootMargin: '0px',
                threshold: 0.2 // Trigger when 20% of the section is visible
            };

            // Create the observer
            const observer = new IntersectionObserver(function(entries, observer) {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        // Add animated class when section comes into view
                        $(entry.target).addClass('animated');
                        
                        // Optional: Unobserve after animation (one-time animation)
                        // observer.unobserve(entry.target);
                    } else {
                        // Optional: Remove class when scrolling away (repeating animation)
                        // $(entry.target).removeClass('animated');
                    }
                });
            }, options);

            // Observe the timeline section
            const timelineSection = document.getElementById('timelineSection');
            observer.observe(timelineSection);

            // Smooth scroll behavior
            $('html').css('scroll-behavior', 'smooth');
        });
    </script>


    {% schema %}

    {
    
    "name":"Custom Start Animation",
    "settings": [],
    "presets": [
        {
        "name":"Custom Star Animation"
        }
    ]
    }
    {% endschema %}
